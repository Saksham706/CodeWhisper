#include <stdio.h>

int main() {
    int a, b;

    // Taking input
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    // Processing
    int sum = a + b;

    // Output
    printf("Sum = %d\n", sum);

    return 0;
}
